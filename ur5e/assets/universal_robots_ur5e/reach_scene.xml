<mujoco model="ur5e_reach">
  <!-- This scene lives next to ur5e.xml on purpose. MuJoCo resolves the robot's
       mesh paths (meshdir="assets" inside ur5e.xml) relative to the directory
       of the top-level scene file, so a scene that includes the robot has to
       sit beside it. Put this one level up and the meshes fail to load. -->
  <include file="ur5e.xml"/>

  <statistic center="0.4 0 0.4" extent="1"/>   <!-- frames the default camera on the arm -->

  <visual>
    <headlight diffuse="0.6 0.6 0.6" ambient="0.3 0.3 0.3" specular="0 0 0"/>
    <global azimuth="120" elevation="-20"/>
  </visual>

  <asset>
    <texture type="2d" name="groundplane" builtin="checker" mark="edge"
             rgb1="0.2 0.3 0.4" rgb2="0.1 0.2 0.3" markrgb="0.8 0.8 0.8"
             width="300" height="300"/>
    <material name="groundplane" texture="groundplane" texuniform="true"
              texrepeat="5 5" reflectance="0.2"/>
  </asset>

  <worldbody>
    <light pos="0 0 1.5" dir="0 0 -1" directional="true"/>
    <geom name="floor" size="0 0 0.05" type="plane" material="groundplane"/>

    <!-- The target the arm must reach. mocap="true" means physics ignores this
         body, but Python can teleport it every episode (data.mocap_pos) to make
         a new random goal. contype/conaffinity 0 make it a ghost, so the arm
         passes through it rather than colliding. -->
    <body name="target" mocap="true" pos="0.5 0 0.5">
      <geom name="target" type="sphere" size="0.025" rgba="0.9 0.2 0.2 0.8"
            contype="0" conaffinity="0"/>
    </body>
  </worldbody>
</mujoco>
